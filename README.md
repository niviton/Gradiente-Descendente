# Gradiente-Descendente
# (opcional, mas recomendado) criar e ativar venv
python -m venv .venv
source .venv/bin/activate

# instalar dependências
pip install numpy torch matplotlib

# executar o script
python treinamento.py